?runif
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (1 == 1) {
# Try and render the file, if you can't instead print an erro (line ~85)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_exploration.html")
# Knit the VisulisationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "VisulisationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
}
source("C:/Users/user/Desktop/Oakleigh_Project/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/Accelerometer_Analysis/Scripts/Script_Main.R")
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (1 == 1) {
# Try and render the file, if you can't instead print an erro (line ~85)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_exploration.html")
# Knit the VisulisationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "VisulisationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
}
setdiff(feature_data, feature_data_other)
ay <- 1,2,3
ay <- c(1,2,3)
ab <- c(1,2,3)
setdiff(ay,ab)
View(feature_data_other)
View(feature_data)
source("C:/Users/user/Desktop/Oakleigh_Project/Accelerometer_Analysis/Scripts/Script_Main.R")
y <- 1
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
print(y)
source("C:/Users/user/Desktop/Oakleigh_Project/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
file.exists(paste0(dataset_name, "_Behaviour_Duration_Report.html")
)
paste0(dataset_name, "_Behaviour_Duration_Report.html")
paste0(base_path, dataset_name, "_Behaviour_Duration_Report.html")
paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html")
file.exists(paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html")))
(file.exists(paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html"))
)
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (!(file.exists(paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html")))) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
generateBD_Report()
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
} else {
print(paste0("Behaviour duration report already exists for ", dataset_name, " in \n", base_path, "/Plots/"))
}
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
generateBD_Report()
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (!(file.exists(paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html")))) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
generateBD_Report()
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
} else {
print(paste0("Behaviour duration report already exists for ", dataset_name, " in /n", base_path, "/Plots/"))
}
cat(paste0("Behaviour duration report already exists for ", dataset_name, " in/n", base_path, "/Plots/"))
cat(paste0("Behaviour duration report already exists for ", dataset_name, " in\n", base_path, "/Plots/"))
cat("")
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (!(file.exists(paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html")))) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
generateBD_Report()
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
} else {
cat(paste0("Behaviour duration report already exists for ", dataset_name, " in\n", base_path, "/Plots/"))
cat("")
cat()
}
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (!(file.exists(paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html")))) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
generateBD_Report()
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
} else {
cat(paste0("Behaviour duration report already exists for ", dataset_name, " in\n", base_path, "/Plots/"))
cat("")
cat(paste0("Please removeo or rename: ", dataset_name, "_Behaviour_Duration_Report.html", " from the plots folder and try again."))
}
cat("\n")
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (!(file.exists(paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html")))) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
generateBD_Report()
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
} else {
cat(paste0("Behaviour duration report already exists for ", dataset_name, " in\n", base_path, "/Plots/"))
cat("\n")
cat(paste0("Please remove or rename: ", dataset_name, "_Behaviour_Duration_Report.html", " from the plots folder and try again."))
}
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (!(file.exists(paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html")))) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
generateBD_Report()
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
} else {
cat(paste0("Behaviour duration report already exists for ", dataset_name, " in\n", base_path, "/Plots/"))
cat("\n\n")
cat(paste0("Please remove or rename: ", dataset_name, "_Behaviour_Duration_Report.html", " from the plots folder and try again."))
}
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (!(file.exists(paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html")))) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
generateBD_Report()
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
} else {
cat(paste0("Behaviour duration report already exists for ", dataset_name,".csv", " in\n", base_path, "/Plots/"))
cat("\n\n")
cat(paste0("Please remove or rename: ", dataset_name, "_Behaviour_Duration_Report.html", " from the plots folder and try again."))
}
debugSource("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
debugSource("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (!(file.exists(paste0(base_path,"/Plots/", dataset_name, "_Behaviour_Duration_Report.html")))) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
generateBD_Report()
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
} else {
cat(paste0("Behaviour duration report already exists for ", dataset_name,".csv", " in:\n", base_path, "/Plots/"))
cat("\n\n")
cat(paste0("Please remove or rename: \n", dataset_name, "_Behaviour_Duration_Report.html", " from the plots folder and try again."))
}
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Main.R", echo=TRUE)
