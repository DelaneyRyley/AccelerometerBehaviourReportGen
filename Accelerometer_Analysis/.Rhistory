fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[5]]
#   #Need to include:
# Report both mean and median
#
# Report max duration, minimum, mean, median and the lower quartile.
#
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Report_Generators.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
View(printer)
View(printer)
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
View(printer)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
View(printer)
printer[[6]]
min(duration_stats$minimum)
x <- (2,2,3,6)
x <- c(2,2,3,6)
min(x)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
View(printer)
printer[[6]]
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
View(printer)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[5]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the lower quartile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
View(printer)
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[5]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the lower quartile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[5]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the lower quartile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
View(printer)
View(printer)
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[5]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the lower quartile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
View(printer)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[5]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the lower quartile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
View(printer)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[5]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the (x)lower quantile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
View(printer)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
View(printer)
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[5]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the (x)lower quantile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
View(printer)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[5]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the (x)lower quantile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[5]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the (x)lower quatile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
View(printer)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
View(printer)
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[7]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the (x)lower quantile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
View(printer)
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[7]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the (x)lower quantile.
#  Report the smallest lower quartile, and the behaviours associated with it.
# Present maybe the lower quartile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
View(printer)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5,
# Stops messages from being produced in the output
message = FALSE,
# Stops warnings from appearing in the output
warning = FALSE)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Get the Behaviour duration plot and the behaviour names of the minimum
printer <- plotBehaviourDuration(data = raw_data, sample_rate = sample_rate)
# Draw the plot on the report
printer[[7]]
printer[[8]]
#   #Need to include:
# Report both mean and median XX
#
# Report (x)max duration, (x)minimum, (x)mean, (x)median and the (x)lower quantile.
#  Report the smallest lower quantile, and the behaviours associated with it.
# Present maybe the lower quantile mean.
#
# needs to include edge cases for multiple behaviours having the same minimum XX
printer[[8]]
