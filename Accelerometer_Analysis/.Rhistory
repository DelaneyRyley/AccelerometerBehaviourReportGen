sample_rate <- species_settings[[dataset_name]]$sample_rate
# Doing the same for overlap variable within our dictionary.
overlap_percent <- species_settings[[dataset_name]]$overlap_percent
# Same for window length
window_length <- species_settings[[dataset_name]]$window_length
# Assign available axes
available_axes <- c("Accelerometer.X", "Accelerometer.Y", "Accelerometer.Z")
# Split test data out and load other data ---------------------------------
source(file.path(base_path, "Scripts", "SplitTestData.R"))
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (1 == 1) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_Behaviour_Duration_Report.html")
# Knit the GenerateBehaviourDurationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "GenerateBehaviourDurationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
}
# currently set to only process a very small number of windows
source(file.path(base_path, "Scripts", "GeneratingFeatures.R"))
plotBehaviourDuration(data = raw_data, frequency = sample_rate)
# Install packages  -------------------------------------------------------
library(data.table)
library(tidyverse)
# Install packages  -------------------------------------------------------
library(data.table)
library(tidyverse)
library(tsfeatures)
library(umap)
library(caret)
library(ggpubr) # for retrieving the legend in one of my plots
# Input the dataset name from the list of dictionaries in Species Settings
dataset_name <- "Studd_Squirrel"
# Set the base path to our directory we are working from.
base_path <- "C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis"
# Here we created a dictionary that that's a list of variables for each species: sample rate and overlap
species_settings <- list(Studd_Squirrel = list(sample_rate = 1, overlap_percent = 20, window_length = 10),
Seal = list(sample_rate = 100, overlap_percent = 20, window_length = 10))
# Here we are going to set the sample rate based on the name from the dictionary and pull the values of that.
sample_rate <- species_settings[[dataset_name]]$sample_rate
# Doing the same for overlap variable within our dictionary.
overlap_percent <- species_settings[[dataset_name]]$overlap_percent
# Same for window length
window_length <- species_settings[[dataset_name]]$window_length
# Assign available axes
available_axes <- c("Accelerometer.X", "Accelerometer.Y", "Accelerometer.Z")
# Split test data out and load other data ---------------------------------
source(file.path(base_path, "Scripts", "SplitTestData.R"))
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (1 == 1) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_Behaviour_Duration_Report.html")
# Knit the GenerateBehaviourDurationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "GenerateBehaviourDurationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
}
# currently set to only process a very small number of windows
source(file.path(base_path, "Scripts", "GeneratingFeatures.R"))
# Subset the data into a smaller amount, "toy data"
data <- data1 %>%
group_by(ID,Activity) %>%
slice(1:5) %>%
as.data.table()
# Main function that calls the others
generateFeatures <- function(window_length, sample_rate, overlap_percent, raw_data, features_type)
{
# multiprocessing
#plan(multisession, workers = availableCores())  # Use parallel processing
# Split raw_data by 'ID' # was by = "ID" before
raw_data_by_id <- split(raw_data, raw_data$ID)
# Process each ID's raw_data
features_by_id <- list()
for (id in unique(raw_data$ID)) {
print(id)
# I changed the way this was subsetted. Was previously raw_data_by_id[[as.character(id)]]
features_by_id[[id]] <- processDataPerID(
id_raw_data = raw_data_by_id[[id]],
features_type,
window_length,
sample_rate,
overlap_percent
)
}
all_features <- do.call(rbind, features_by_id)
#plan(sequential)  # Return to sequential execution
all_features <- rbindlist(features_by_id)
return(all_features)
}
# Subset the data into a smaller amount, "toy data"
data <- data1 %>%
group_by(ID,Activity) %>%
slice(1:5) %>%
as.data.table()
if (file.exists(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_data.csv"))))
if (file.exists(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_dat.csv"))))
if (file.exists(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_dat.csv"))))
if (file.exists(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_dat.csv"))))
x
# currently set to only process a very small number of windows
source(file.path(base_path, "Scripts", "GeneratingFeatures.R"))
{
feature_data_other <- fread(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_data.csv")))
} else
if (file.exists(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_dat.csv"))))
{
feature_data_other <- fread(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_data.csv")))
} else {
# Read the data from our training/validation data set.
data1 <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_other.csv")))
# Subset the data into a smaller amount, "toy data"
data <- data1 %>%
group_by(ID,Activity) %>%
slice(1:5) %>%
as.data.table()
feature_data_other <- generateFeatures(window_length, sample_rate, overlap_percent,
raw_data = data,
features_type = c("statistical", "timeseries"))
fwrite(feature_data_other, file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_data.csv")))
}
if (file.exists(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_dat.csv"))))
if (file.exists(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_dat.csv"))))
if (file.exists(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_dat.csv")))) {
feature_data_other <- fread(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_data.csv")))
} else {
# Read the data from our training/validation data set.
data1 <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Subset the data into a smaller amount, "toy data"
data <- data1 %>%
group_by(ID,Activity) %>%
slice(1:5) %>%
as.data.table()
feature_data_other <- generateFeatures(window_length, sample_rate, overlap_percent,
raw_data = data,
features_type = c("statistical", "timeseries"))
fwrite(feature_data_other, file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_data.csv")))
}
if (file.exists(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_dat.csv")))) {
feature_data_other <- fread(file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_data.csv")))
} else {
# Read the data from our training/validation data set.
data1 <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Subset the data into a smaller amount, "toy data"
data <- data1 %>%
group_by(ID,Activity) %>%
slice(1:5) %>%
as.data.table()
feature_data_other <- generateFeatures(window_length, sample_rate, overlap_percent,
raw_data = data,
features_type = c("statistical", "timeseries"))
fwrite(feature_data_other, file.path(base_path, "Data", "Feature_Data", paste0(dataset_name, "_other_feature_data.csv")))
}
# Install packages  -------------------------------------------------------
library(data.table)
library(tidyverse)
library(tsfeatures)
library(umap)
library(caret)
library(ggpubr) # for retrieving the legend in one of my plots
# Input the dataset name from the list of dictionaries in Species Settings
dataset_name <- "Studd_Squirrel"
# Set the base path to our directory we are working from.
base_path <- "C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis"
# Here we created a dictionary that that's a list of variables for each species: sample rate and overlap
species_settings <- list(Studd_Squirrel = list(sample_rate = 1, overlap_percent = 20, window_length = 10),
Seal = list(sample_rate = 100, overlap_percent = 20, window_length = 10))
# Here we are going to set the sample rate based on the name from the dictionary and pull the values of that.
sample_rate <- species_settings[[dataset_name]]$sample_rate
# Doing the same for overlap variable within our dictionary.
overlap_percent <- species_settings[[dataset_name]]$overlap_percent
# Same for window length
window_length <- species_settings[[dataset_name]]$window_length
# Assign available axes
available_axes <- c("Accelerometer.X", "Accelerometer.Y", "Accelerometer.Z")
# Split test data out and load other data ---------------------------------
source(file.path(base_path, "Scripts", "SplitTestData.R"))
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (1 == 1) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_Behaviour_Duration_Report.html")
# Knit the GenerateBehaviourDurationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "GenerateBehaviourDurationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
}
# currently set to only process a very small number of windows
source(file.path(base_path, "Scripts", "GeneratingFeatures.R"))
dataset_name <- ""
if (dataset_name == "") {
print(" Dataset defined as blank.")
}
file.exists(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
# Attempt to load in the data ---------------------------------------------
raw_data_exists <- FALSE
paste0(dataset_name, "_Training.csv")
# Set the variables of our other chunks
knitr::opts_chunk$set(
# Set so that for all of our plots, it won't include the code.
echo = FALSE,
# Set the global figure width.
fig.width = 5.625,
# Set the global figure height.
fig.height = 4.5)
# We need to check if the user has all the right packages installed and if they don't install them.
# At this point we're just loading the packages but we need to change that.
library(data.table)
library(purrr)
library(tidyverse)
library(kableExtra)
# Source all of our functions
source(file.path(params$base_path, "Scripts", "Functions.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
file.path(params$base_path, "Scripts", "LoadData.R")
---
# Creating and assigning our global variables
title: "Behaviour Duration Report"
---
# Creating and assigning our global variables
title: "Behaviour Duration Report"
# Input the dataset name from the list of dictionaries in Species Settings
dataset_name <- "Studd_Squirrel"
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (1 == 1) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_Behaviour_Duration_Report.html")
# Knit the GenerateBehaviourDurationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "GenerateBehaviourDurationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
}
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (1 == 1) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_Behaviour_Duration_Report.html")
# Knit the GenerateBehaviourDurationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "GenerateBehaviourDurationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
}
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
# Knit the GenerateBehaviourDurationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "GenerateBehaviourDurationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (1 == 1) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_Behaviour_Duration_Report.html")
# Knit the GenerateBehaviourDurationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "GenerateBehaviourDurationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
}
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_Behaviour_Duration_Report.html")
# Knit the GenerateBehaviourDurationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "GenerateBehaviourDurationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
# Create a check that sees if the file has already been created, or created recently.
# For now its not checking anything. Change later.
if (1 == 1) {
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_Behaviour_Duration_Report.html")
# Knit the GenerateBehaviourDurationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "GenerateBehaviourDurationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
}
# Try and render the file, if you can't instead print an error (line ~82)
tryCatch(
{
# Define the output directory and file
output_dir <- file.path(base_path, "Plots")
# Define what we will be calling
output_file <- paste0(dataset_name, "_Behaviour_Duration_Report.html")
# Knit the GenerateBehaviourDurationReport.Rmd file as an HTML report
rmarkdown::render(
# Input file that we are going to render
input = file.path(base_path, "Scripts", "GenerateBehaviourDurationReport.Rmd"),
# Output as a html document.
output_format = "html_document",
# Define the output file based on the previous output file variable
output_file = output_file,  # File name only
output_dir = output_dir,   # Directory for saving the file
params = list(
base_path = base_path,
dataset_name = dataset_name,
sample_rate = sample_rate
)
)
# Success message with full path
message("Exploration report saved to: ", file.path(output_dir, output_file))
}, error = function(e) {
message("Error in making the data exploration report: ", e$message)
stop()
})
# Source the Load Data script and attempt to load in the data
source(file.path(params$base_path, "Scripts", "LoadData.R"))
raw_data <- fread(file.path(base_path, "Data", "Other_Data", paste0(dataset_name, "_Training.csv")))
plotBehaviourDuration(data = raw_data, frequency = sample_rate)
source("C:/Users/user/Desktop/Oakleigh_Project/AccelerometerData/Accelerometer_Analysis/Scripts/Script_Main.R")
